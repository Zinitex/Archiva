$kind: http-request
url: "{{base_url}}/documents/1/approve"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "status": "approved",
      "comment": "Disetujui untuk dipublikasikan"
    }
order: 3000
